// 按顺序依次定义两端线网、多端线网和bus线网组
1 				// 本网表共含有1个两端线网(红色线网)，若没有两端线网，此处应为0
0 0 1 0 1 1		// 第1-2个数字（0，0）表示第一个端点所在的PL的坐标，第3个数字2表示该PL的端口序号为1
				// 第4-5个数字（0，1）表示第一个端点所在的PL的坐标，第3个数字1表示该PL的端口序号为1
				// 每三个数字可以确定一个PL的IO端口（即线网的一个端点）：第1-2个数字是PL的坐标，第3个数字是PL的IO端口序号
				// 每一行表示一个两端线网

1				// 本网表共含有1个多端线网(黄色线网)，若没有多端线网，此处应为0
3				// 以下每个线网包含3个端点
0 0 0 0 1 0 0 2 0	// 第一个端点是坐标为（0,0）的PL上序号为0的IO端口
				// 第二个端点是坐标为（0,1）的PL上序号为0的IO端口
				// 第三个端点是坐标为（0,2）的PL上序号为0的IO端口				

1				// bus线网组的总数，若没有bus线网，此处应为0
0 2				// 编号为0的bus线网组共包含2个两端线网
0 0 2 0 2 2		// 编号为0的bus线网组中的第一个两端线网
0 0 3 0 2 3		// 编号为0的bus线网组中的第二个两端线网